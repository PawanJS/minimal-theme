import ReactDOM from 'react-dom';
import React from 'react';
import CartToolTip from './components/CartToolTip';

const cartIconContainer = document.getElementById('react-cart-tool-tip');

// cartIconContainer && ReactDOM.render(<CartToolTip />, cartIconContainer);
